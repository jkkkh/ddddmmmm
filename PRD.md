# 产品需求文档（PRD）：私域与公域订单同步授权与绑定

| 文档版本 | 修改日期 | 修改人 | 修改说明 |
|----------|----------|--------|----------|
| v1.0     | 2026-08-31 | AI助手 | 初稿 |

---

## 1. 项目背景

企业品牌方需要在私域（小程序/小店）和公域（直播间/平台）之间同步用户的交易订单，以便统一管理订单、售后服务及会员权益。同步前必须获得用户授权，并确认公域ID与私域会员号的绑定关系。

当前用户旅程：公域直播间点击商品 → 跳转私域商品详情页 → 登录会员 → 加购 → 支付 → 生成订单。若当前公域登录ID与已绑定关系不一致，需提示用户是否重新绑定。

## 2. 产品目标

- 在不打断购买主流程的前提下，获取用户订单同步授权。
- 建立并维护公域ID与私域会员号的一一绑定关系（或多绑定，需业务确认）。
- 当绑定关系不一致时，引导用户确认或更新。
- 用户可随时查看、修改、撤回授权与绑定。
- 订单支付成功后能够可靠同步至公域。

## 3. 名词定义

| 术语 | 定义 |
|------|------|
| 公域ID | 用户在公域平台（如直播间、视频号等）的唯一标识，由平台提供，需通过安全方式获取。 |
| 私域会员号 | 用户在品牌私域小程序/小店注册的会员唯一标识。 |
| 绑定关系 | 公域ID与私域会员号的映射关系，用于确定订单同步目标。 |
| 订单同步 | 将私域产生的交易订单数据推送至公域平台，或关联至公域账号。 |
| 授权协议 | 用户同意品牌方同步其订单信息的法律文本，需用户主动勾选同意。 |

## 4. 用户旅程与功能流程

### 4.1 主流程

1. 用户在公域直播间点击商品，跳转至私域商品详情页。
2. 用户浏览商品，点击“立即购买”或“加入购物车”。
3. 系统检测私域会员登录状态：
   - 未登录 → 引导登录会员（登录成功后进入步骤4）
   - 已登录 → 直接进入步骤4
4. 系统检测“公域ID与私域会员号绑定关系”及“授权状态”。
5. 根据检测结果，触发对应弹窗或提示（见5.2场景）。
6. 用户进入订单确认页。
7. 订单确认页展示订单同步状态，并提供管理入口。
8. 用户点击“提交订单”时，若未授权或未绑定，进行轻量确认。
9. 支付成功后，系统执行订单同步。

### 4.2 关键决策点

- **弹窗主位置**：用户登录会员成功后、进入加购/结算流程前（即步骤4之后）。
- **兜底位置**：订单确认页常驻展示同步状态，并提供快捷操作。
- **提交订单前**：若同步条件不满足，轻提示但不强制阻断。

## 5. 页面与弹窗详细设计

### 5.1 商品详情页

- **不主动弹窗**，避免打断浏览。

### 5.2 登录后触发弹窗（根据场景A/B/C）

#### 场景A：未绑定关系

**触发条件**：已登录私域会员，但当前公域ID未绑定任何私域会员号。

**弹窗标题**：同步订单需要您的授权

**弹窗内容**：
> 为了将您的订单信息在公域与私域之间同步，便于订单管理、售后服务及会员权益发放，我们需要：
> 1. 将您当前公域账号与私域会员号绑定；
> 2. 授权我们同步您的交易订单信息。
> 请阅读并同意《订单同步授权协议》。
> [ ] 我已阅读并同意《订单同步授权协议》  （默认不勾选）

**交互按钮**：
- 主按钮：`同意并绑定`（在勾选协议后可用）
- 次按钮：`暂不`

**选择结果**：
- 同意并绑定：完成绑定，写入授权记录，继续原购买流程。
- 暂不：继续购物，订单同步不生效；订单确认页展示“未授权同步”提示，并提供再次授权入口。

#### 场景B：已绑定且当前公域ID一致

**触发条件**：当前公域ID与私域会员号已绑定，且用户已授权过同步。

**交互策略**：
- 不主动弹窗。
- 订单确认页展示“已授权同步”状态（见5.3）。
- 若授权协议版本更新，则需重新获取用户同意（触发类似场景A的弹窗，但文案强调协议更新）。

#### 场景C：已绑定但当前公域ID不一致

**触发条件**：当前登录的公域ID与已绑定的公域ID不同（例如已绑定A，当前为B）。

**弹窗标题**：检测到当前公域账号与已绑定账号不一致

**弹窗内容**：
> 您当前登录的公域账号为：B
> 已绑定的公域账号为：A
> 是否将绑定关系更新为当前账号B？
> 更新后，后续订单将同步至账号B。

**交互按钮**：
- 主按钮：`确认重新绑定`
- 次按钮：`保持原绑定`

**选择结果**：
- 确认重新绑定：更新绑定关系为当前公域ID，订单同步至当前账号。
- 保持原绑定：绑定关系不变。系统提示：
  > 当前订单仍将同步至原绑定账号A。如果账号A已失效或您希望同步至当前账号，请重新绑定。
  订单确认页需显著提示当前同步目标账号。

### 5.3 订单确认页（常驻模块）

订单确认页需展示“订单同步状态栏”，样式示例：
  > 订单同步状态：已开启 / 未开启
  > 同步目标公域账号：A / 当前账号B / 未绑定
  > 授权协议：已同意 / 待授权
  > [管理] （点击进入授权与绑定管理页）


- 若用户未授权或未绑定，状态栏显示`未开启`，并提供快捷按钮：`立即授权并绑定` 或 `查看授权协议`。
- 点击“提交订单”时，若同步条件不满足，弹出轻量确认弹窗：
  > 当前订单将不会同步至公域，可能影响后续售后或会员权益。是否继续提交？
  - 按钮：`继续提交`、`返回授权`

### 5.4 会员中心 - 账号绑定与授权管理页

用户可进入此页面进行以下操作：
- 查看当前绑定的公域账号。
- 解绑（需二次确认）。
- 重新绑定（触发场景C或A的流程）。
- 撤回订单同步授权（撤回后，后续订单不再同步，历史订单不受影响）。
- 查看授权协议内容。

## 6. 状态与数据模型

### 6.1 后端记录字段

| 字段 | 类型 | 说明 |
|------|------|------|
| member_id | string | 私域会员号 |
| public_id | string | 公域ID（当前绑定） |
| bind_status | enum | active / unbound |
| auth_status | enum | authorized / unauthorized |
| auth_time | datetime | 最近授权时间 |
| auth_protocol_version | string | 授权协议版本号 |
| auth_scope | string | 授权范围描述 |

### 6.2 状态组合与触发逻辑

| 绑定状态 | 授权状态 | 当前公域ID与绑定ID是否一致 | 触发动作 |
|----------|----------|----------------------------|----------|
| 未绑定 | 未授权 | - | 场景A弹窗 |
| 已绑定 | 已授权 | 是 | 无弹窗，订单页显示已开启 |
| 已绑定 | 已授权 | 否 | 场景C弹窗 |
| 已绑定 | 未授权 | 是/否 | 场景A弹窗（侧重授权，绑定可保持不变） |
| 已绑定 | 已授权，但协议版本更新 | 是 | 弹窗要求重新授权 |

## 7. 异常与边界场景

### 7.1 无法获取公域ID

- 用户直接打开私域小程序而非从公域进入。
- 处理：不主动弹窗；订单确认页提示“未检测到公域环境，订单将不同步”；用户可进入会员中心手动绑定。

### 7.2 公域ID获取安全性

- 公域ID必须通过官方跳转参数、平台开放接口或安全签名获取，不可仅依赖前端透传，防止伪造。
- 如无法验证合法性，视为未获取到公域ID。

### 7.3 弹窗疲劳控制

- 用户选择“暂不”后，7天内不再主动弹出相同弹窗（除非用户主动触发）。
- 仅订单确认页持续展示状态及入口。

### 7.4 同步失败

- 支付成功但订单同步失败时：异步重试；订单详情页显示同步状态（同步中/失败/成功）；提供“手动同步”按钮；若因绑定错误同步至错误账号，提供客服和纠错流程。

### 7.5 多公域入口

- 若业务支持多个公域平台（如视频号、公众号），需明确一个私域会员号是否可绑定多个公域ID。当前设计默认一对一绑定，若需多绑定，需扩展绑定表并定义默认同步目标。

## 8. 交互细节与文案规范

- 弹窗标题简洁明确，如“同步订单需要您的授权”。
- 正文避免大量法律术语，但必须包含《订单同步授权协议》链接。
- 按钮文案避免模糊的“确定/取消”，使用“同意并绑定”“暂不”“确认重新绑定”“保持原绑定”等。
- 授权协议必须单独勾选，不能默认勾选。
- 重新绑定属于高风险操作，使用警示色或二次确认。

## 9. 验收标准（供AI生成demo参考）

- 用户从公域进入私域，点击购买并登录后，能根据绑定/授权状态正确弹出对应弹窗。
- 用户同意并绑定后，订单确认页显示“已开启”状态，提交订单无额外提示。
- 用户拒绝授权后，订单确认页显示“未开启”，提交订单时出现轻量确认。
- 当公域ID不一致时，用户选择“重新绑定”后，绑定关系更新，订单同步至新账号。
- 会员中心可以解绑、重新绑定、撤回授权。
- 弹窗疲劳控制生效：选择“暂不”后7天内不重复弹窗。

---

## 附录：授权协议要点（供生成协议内容时参考）

- 授权范围：订单信息（商品、金额、时间等）同步至公域平台。
- 授权期限：用户撤回前有效。
- 用户权利：可随时撤回，撤回不影响之前已同步订单。
- 数据安全：品牌方需遵守相关隐私法规，保障信息安全。

---
