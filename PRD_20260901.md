# 产品需求文档（PRD）：私域与公域订单同步授权与绑定（含优惠券场景）

| 文档版本 | 修改日期 | 修改人 | 修改说明 |
|----------|----------|--------|----------|
| v1.1     | 2026-09-01 | AI助手 | 新增优惠券领取与使用授权绑定逻辑 |

---

## 1. 项目背景

企业品牌方需要在私域（小程序/小店）和公域（直播间/平台）之间同步用户的交易订单，以便统一管理订单、售后服务及会员权益。同步前必须获得用户授权，并确认公域ID与私域会员号的绑定关系。

同时，品牌方在公域发放优惠券，用户领取及使用公域优惠券时，同样需要建立授权与绑定关系，确保优惠券归属明确、可追溯，防止滥用。

当前用户旅程：公域直播间点击商品或领取优惠券 → 跳转私域页面 → 登录会员 → 加购/领券 → 支付/使用优惠券 → 生成订单。若当前公域登录ID与已绑定关系不一致，需提示用户是否重新绑定。

## 2. 产品目标

- 在不打断购买主流程的前提下，获取用户订单同步授权。
- 建立并维护公域ID与私域会员号的一一绑定关系（或多绑定，需业务确认）。
- 当绑定关系不一致时，引导用户确认或更新。
- 用户可随时查看、修改、撤回授权与绑定。
- 订单支付成功后能够可靠同步至公域。
- 公域优惠券必须在授权且绑定关系明确时方可领取与使用。

## 3. 名词定义

| 术语 | 定义 |
|------|------|
| 公域ID | 用户在公域平台（如直播间、视频号等）的唯一标识，由平台提供，需通过安全方式获取。 |
| 私域会员号 | 用户在品牌私域小程序/小店注册的会员唯一标识。 |
| 绑定关系 | 公域ID与私域会员号的映射关系，用于确定订单同步目标。 |
| 订单同步 | 将私域产生的交易订单数据推送至公域平台，或关联至公域账号。 |
| 授权协议 | 用户同意品牌方同步其订单信息的法律文本，需用户主动勾选同意。 |
| 公域优惠券 | 通过公域渠道（直播间、短视频等）发放的优惠券，需关联公域账号使用。 |
| 私域优惠券 | 在私域内发放的优惠券，不强制要求公域绑定。 |

## 4. 用户旅程与功能流程

### 4.1 主流程（购买商品）

1. 用户在公域直播间点击商品，跳转至私域商品详情页。
2. 用户浏览商品，点击“立即购买”或“加入购物车”。
3. 系统检测私域会员登录状态：
   - 未登录 → 引导登录会员（登录成功后进入步骤4）
   - 已登录 → 直接进入步骤4
4. 系统检测“公域ID与私域会员号绑定关系”及“授权状态”。
5. 根据检测结果，触发对应弹窗或提示（见5.2场景A/B/C）。
6. 用户进入订单确认页。
7. 订单确认页展示订单同步状态，并提供管理入口。
8. 用户点击“提交订单”时，若未授权或未绑定，进行轻量确认。
9. 支付成功后，系统执行订单同步。

### 4.2 主流程（领取公域优惠券）

1. 用户在公域直播间点击“领取优惠券”，跳转至私域领券页面。
2. 系统检测私域会员登录状态：
   - 未登录 → 引导登录会员（登录成功后进入步骤3）
   - 已登录 → 直接进入步骤3
3. 系统检测“公域ID与私域会员号绑定关系”及“授权状态”。
4. 根据检测结果，触发对应弹窗（见5.2场景A/C）。
5. 用户完成授权与绑定后，优惠券发放至用户账户。
6. 若用户拒绝授权/绑定，则领取失败，并提示原因。

### 4.3 关键决策点

- **弹窗主位置**：用户登录会员成功后、进入加购/结算流程前，以及领取公域优惠券时。
- **兜底位置**：订单确认页常驻展示同步状态，并提供快捷操作。
- **提交订单前**：若同步条件不满足或使用了受限优惠券，进行轻量确认或拦截。
- **使用公域优惠券时**：在订单确认页选择优惠券后，校验授权绑定状态，不满足则优惠券不可用。

## 5. 页面与弹窗详细设计

### 5.1 商品详情页 / 领券页面

- **商品详情页**：不主动弹窗，避免打断浏览。
- **领券页面**：若检测到需要授权/绑定，则直接弹出对应弹窗；否则直接领取。

### 5.2 授权与绑定弹窗（根据场景A/B/C）

#### 场景A：未绑定关系（适用于购买流程和领券流程）

**触发条件**：已登录私域会员，但当前公域ID未绑定任何私域会员号。

**弹窗标题**：同步订单需要您的授权（领券时标题可改为“领取优惠券需要授权”）

**弹窗内容**：
> 为了将您的订单信息在公域与私域之间同步（或为了将优惠券准确发放至您的账号），我们需要：
> 1. 将您当前公域账号与私域会员号绑定；
> 2. 授权我们同步您的交易订单信息。
> 请阅读并同意《订单同步授权协议》。
> [ ] 我已阅读并同意《订单同步授权协议》  （默认不勾选）

**交互按钮**：
- 主按钮：`同意并绑定`（在勾选协议后可用）
- 次按钮：`暂不`（购买流程） / `取消领取`（领券流程）

**选择结果**：
- 同意并绑定：完成绑定，写入授权记录，继续原购买或领券流程。
- 暂不/取消领取：购买流程继续（订单同步不生效）；领券流程直接失败，提示“未授权绑定，无法领取公域优惠券”。

#### 场景B：已绑定且当前公域ID一致（适用于购买流程）

**触发条件**：当前公域ID与私域会员号已绑定，且用户已授权过同步。

**交互策略**：
- 不主动弹窗。
- 订单确认页展示“已授权同步”状态（见5.3）。
- 若授权协议版本更新，则需重新获取用户同意（触发类似场景A的弹窗，但文案强调协议更新）。

#### 场景C：已绑定但当前公域ID不一致（适用于购买流程和领券流程）

**触发条件**：当前登录的公域ID与已绑定的公域ID不同（例如已绑定A，当前为B）。

**弹窗标题**：检测到当前公域账号与已绑定账号不一致

**弹窗内容**：
> 您当前登录的公域账号为：B
> 已绑定的公域账号为：A
> 是否将绑定关系更新为当前账号B？
> 更新后，后续订单将同步至账号B（或优惠券将发放至账号B）。

**交互按钮**：
- 主按钮：`确认重新绑定`
- 次按钮：`保持原绑定`（购买流程） / `取消领取`（领券流程）

**选择结果**：
- 确认重新绑定：更新绑定关系为当前公域ID，继续流程。
- 保持原绑定：购买流程中绑定关系不变，系统提示同步目标为原账号；领券流程中优惠券将发放至原绑定账号A（需二次确认）。若用户不愿发放至A，则取消领取。
- 取消领取（领券）：直接关闭弹窗，领取失败。

### 5.3 订单确认页（常驻模块）

订单确认页需展示“订单同步状态栏”，样式示例：
  > 订单同步状态：已开启 / 未开启
  > 同步目标公域账号：A / 当前账号B / 未绑定
  > 授权协议：已同意 / 待授权
  > [管理] （点击进入授权与绑定管理页）


- 若用户未授权或未绑定，状态栏显示`未开启`，并提供快捷按钮：`立即授权并绑定` 或 `查看授权协议`。
- 点击“提交订单”时，若同步条件不满足，弹出轻量确认弹窗：
  > 当前订单将不会同步至公域，可能影响后续售后或会员权益。是否继续提交？
  - 按钮：`继续提交`、`返回授权`

### 5.4 订单确认页 - 优惠券使用校验

当用户在订单确认页选择使用优惠券时，系统需校验优惠券类型：

- **私域优惠券**：不校验公域授权与绑定，直接可用。
- **公域优惠券**：执行以下校验：
  1. 用户已授权（auth_status = authorized）
  2. 绑定关系存在（bind_status = active）
  3. 当前公域ID与绑定ID一致（若可获取公域ID）
  4. 若任一条件不满足，该优惠券不可选择，并在优惠券列表置灰，显示原因：
     - 未授权：“该优惠券需要授权订单同步后使用，[去授权]”
     - 未绑定或ID不一致：“该优惠券需要绑定当前公域账号后使用，[去绑定]”
  5. 用户点击“去授权”或“去绑定”，弹出相应授权绑定弹窗（场景A或C），完成后返回订单确认页，优惠券恢复可用。
  6. 若用户不愿授权绑定，则无法使用该公域优惠券，可选择不使用优惠券继续提交订单（需走5.3的同步状态确认）。

### 5.5 会员中心 - 账号绑定与授权管理页

用户可进入此页面进行以下操作：
- 查看当前绑定的公域账号。
- 解绑（需二次确认，解绑后公域优惠券可能无法使用，需提示）。
- 重新绑定（触发场景C或A的流程）。
- 撤回订单同步授权（撤回后，后续订单不再同步，历史订单不受影响；已领取的公域优惠券可能失效，需提示）。
- 查看授权协议内容。

## 6. 状态与数据模型

### 6.1 后端记录字段

| 字段 | 类型 | 说明 |
|------|------|------|
| member_id | string | 私域会员号 |
| public_id | string | 公域ID（当前绑定） |
| bind_status | enum | active / unbound |
| auth_status | enum | authorized / unauthorized |
| auth_time | datetime | 最近授权时间 |
| auth_protocol_version | string | 授权协议版本号 |
| auth_scope | string | 授权范围描述 |

**优惠券相关扩展字段**（建议在优惠券系统中增加）：
| 字段 | 类型 | 说明 |
|------|------|------|
| coupon_id | string | 优惠券唯一标识 |
| coupon_type | enum | public / private（公域/私域） |
| require_public_bind | boolean | 是否要求公域绑定（公域券为true） |
| bound_public_id | string | 领取时绑定的公域ID（用于核销校验） |

### 6.2 状态组合与触发逻辑（购买流程）

| 绑定状态 | 授权状态 | 当前公域ID与绑定ID是否一致 | 触发动作 |
|----------|----------|----------------------------|----------|
| 未绑定 | 未授权 | - | 场景A弹窗 |
| 已绑定 | 已授权 | 是 | 无弹窗，订单页显示已开启 |
| 已绑定 | 已授权 | 否 | 场景C弹窗 |
| 已绑定 | 未授权 | 是/否 | 场景A弹窗（侧重授权，绑定可保持不变） |
| 已绑定 | 已授权，但协议版本更新 | 是 | 弹窗要求重新授权 |

### 6.3 状态组合与触发逻辑（领券流程）

| 绑定状态 | 授权状态 | 当前公域ID与绑定ID是否一致 | 触发动作 |
|----------|----------|----------------------------|----------|
| 未绑定 | 未授权 | - | 场景A弹窗，同意后才能领券 |
| 已绑定 | 已授权 | 是 | 直接领券 |
| 已绑定 | 已授权 | 否 | 场景C弹窗，必须重新绑定或确认发放至原账号 |
| 已绑定 | 未授权 | 是/否 | 场景A弹窗，同意授权后才能领券 |
| 已绑定 | 已授权，但协议版本更新 | 是 | 弹窗要求重新授权后才能领券 |

### 6.4 优惠券使用校验逻辑

在订单确认页选择公域优惠券时，校验以下条件全部满足才允许使用：
- `auth_status = authorized`
- `bind_status = active`
- `当前公域ID = bound_public_id`（或绑定关系中的public_id）
- 授权协议版本为最新

任一条件不满足，优惠券不可选，并给出相应提示与操作入口。

## 7. 异常与边界场景

### 7.1 无法获取公域ID

- 用户直接打开私域小程序而非从公域进入。
- 处理：
  - 购买流程：不主动弹窗；订单确认页提示“未检测到公域环境，订单将不同步”；用户可进入会员中心手动绑定。
  - 领券流程：若从非公域环境进入领券页，公域优惠券不可领取，提示“请在公域渠道领取该优惠券”。
  - 使用优惠券：若用户绑定了公域ID但当前无法获取公域ID（如直接打开小程序），则优惠券可能不可用，系统提示“无法验证公域环境，暂不能使用公域优惠券”。

### 7.2 公域ID获取安全性

- 公域ID必须通过官方跳转参数、平台开放接口或安全签名获取，不可仅依赖前端透传，防止伪造。
- 如无法验证合法性，视为未获取到公域ID。

### 7.3 弹窗疲劳控制

- 用户选择“暂不”后，7天内不再主动弹出相同弹窗（除非用户主动触发）。
- 仅订单确认页持续展示状态及入口。
- 领券场景：每次领券时都会检测，若未授权则每次都会弹出，无疲劳控制（因为领券是即时操作，需要明确授权）。

### 7.4 同步失败

- 支付成功但订单同步失败时：异步重试；订单详情页显示同步状态（同步中/失败/成功）；提供“手动同步”按钮；若因绑定错误同步至错误账号，提供客服和纠错流程。

### 7.5 多公域入口

- 若业务支持多个公域平台（如视频号、公众号），需明确一个私域会员号是否可绑定多个公域ID。当前设计默认一对一绑定，若需多绑定，需扩展绑定表并定义默认同步目标，且优惠券需指定可使用的公域范围。

### 7.6 用户解绑或撤回授权对已领优惠券的影响

- 用户解绑或撤回授权后，已领取但未使用的公域优惠券应标记为“不可用”，并在券列表显示原因。
- 用户重新绑定或授权后，未过期的公域优惠券可恢复可用。
- 已使用的优惠券不受影响。

## 8. 交互细节与文案规范

- 弹窗标题简洁明确，如“同步订单需要您的授权”“领取优惠券需要授权”“检测到当前公域账号与已绑定账号不一致”。
- 正文避免大量法律术语，但必须包含《订单同步授权协议》链接。
- 按钮文案避免模糊的“确定/取消”，使用“同意并绑定”“暂不”“确认重新绑定”“保持原绑定”“取消领取”等。
- 授权协议必须单独勾选，不能默认勾选。
- 重新绑定属于高风险操作，使用警示色或二次确认。
- 优惠券不可用时，提示需明确原因和操作路径，例如“该券需绑定公域账号后使用 [去绑定]”。

## 9. 验收标准（供AI生成demo参考）

### 基础授权绑定
- 用户从公域进入私域，点击购买并登录后，能根据绑定/授权状态正确弹出对应弹窗。
- 用户同意并绑定后，订单确认页显示“已开启”状态，提交订单无额外提示。
- 用户拒绝授权后，订单确认页显示“未开启”，提交订单时出现轻量确认。
- 当公域ID不一致时，用户选择“重新绑定”后，绑定关系更新，订单同步至新账号。
- 会员中心可以解绑、重新绑定、撤回授权。
- 弹窗疲劳控制生效：选择“暂不”后7天内不重复弹窗。

### 优惠券领取
- 用户从公域进入领券页，未授权绑定时，弹出授权绑定弹窗，同意后才能成功领取公域优惠券。
- 用户拒绝授权绑定，领取失败并提示原因。
- 当公域ID不一致时，用户选择重新绑定后才能领取；选择保持原绑定则需明确告知发放至原账号；选择取消则领取失败。

### 优惠券使用
- 在订单确认页选择公域优惠券时，若未授权或绑定不一致，优惠券置灰不可选，并显示提示及操作入口。
- 用户通过提示完成授权/绑定后，返回订单页，优惠券变为可选。
- 若用户不满足条件，可以不使用优惠券直接提交订单（需经过订单同步状态确认）。
- 用户撤回授权或解绑后，已领取未使用的公域优惠券变为不可用。

---

## 附录：授权协议要点（供生成协议内容时参考）

- 授权范围：订单信息（商品、金额、时间等）同步至公域平台，以及优惠券的发放与核销信息关联。
- 授权期限：用户撤回前有效。
- 用户权利：可随时撤回，撤回不影响之前已同步订单，但可能影响未使用公域优惠券的使用。
- 数据安全：品牌方需遵守相关隐私法规，保障信息安全。

---